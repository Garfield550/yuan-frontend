export interface OverviewData {
  circSupply?: string,
  curPrice?: number,
  nextRebase?: number,
  nextRebable?: boolean,
  targetPrice?: string | number,
  totalSupply?: string,
  scalingFactor?: string
}